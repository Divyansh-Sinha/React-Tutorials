export const setUser = (user) => ({
    type: 'SET_USER',
    payload: user,
  });
  
  export const setName = (name) => ({
    type: 'SET_NAME',
    payload: name,
  });
  
  export const setUsers = (users) => ({
    type: 'SET_USERS',
    payload: users,
  });